package controlador;

import modelo.Cita;
import modelo.Hospital;
import modelo.Mantenimientos;

public class Controlador {
    public void iniciar() {
        /*ejemplo
        Hospital hospital = new Hospital("Nombre Hospital", "Dirección");
        Mantenimientos mantenimientos = new Mantenimientos("Tipo Mantenimiento", 5);

        int cantidadInstrumentos = 5;
        double precioAfilado = mantenimientos.afilar(cantidadInstrumentos);
        double precioDesmanchado = mantenimientos.desmanchar(cantidadInstrumentos);
        double precioAlineacion = mantenimientos.alineacion(cantidadInstrumentos);
        double precioCorreccionPuntas = mantenimientos.correccionPuntas(cantidadInstrumentos);
        double precioPerforacion = mantenimientos.perforacion(cantidadInstrumentos);

        String dia = "Lunes";
        Cita cita = hospital.agendarCita(dia);
        System.out.println("Cita agendada para el día: " + cita.getDia() + " a las " + cita.getHora());*/
    }

}
